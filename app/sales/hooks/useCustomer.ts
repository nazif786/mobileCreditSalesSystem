import { useQuery } from "react-query";

const useCustomer = () => {
  // const customerQuery = useQuery({
  //   queryKey:["customers"],
  //   queryFn: ()=>
  // });
  return;
};

export default useCustomer;
